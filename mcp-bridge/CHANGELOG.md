## [2.1.1] - 2026-08-24

### 修复

- EDA 存储运行时在启动或重连期间不可用时，保护连接状态持久化操作。
- 通过共享绑定解析器获取实时 EDA 运行时，确保状态读写兼容受支持的 EDA 全局布局。
- 用原生 MCP Server 安装流程替换过时的 MCP Hub 安装说明。

## [2.1.0] - 2026-08-23

### 新增

- 修复运行时 EDA 绑定解析，使专用 PCB、图层、网络和 DRC 处理器在 EDA Pro 3.2.181 上使用与 `api_invoke` 相同的 SDK 对象。
- 新增受确认保护的 `schematic_pages_manage`，支持创建、复制、重命名和完整验证后的页面重排；有意不提供页面删除。
- 新增受限的官方制造导出，包括飞针测试和自动布线/布局 JSON 文件。
- 新增只读 `simulation_model` 搜索，可选 Ngspice/SimulIDE 过滤。
- 新增 `schematic_document_action`，支持受限的原理图坐标/区域检查、选择、图元查询、导航、保存和导入。
- 新增只读当前画布快照、库来源发现，以及 3D 模型/可复用模块/Panel 库搜索和读取。
- 通过 `workspace_query` 新增只读工作区、团队、工程和文件夹发现。
- 通过 `design_source_export` 新增受限只读文档和封装源文件导出，并在 `project_info` 中提供可选 Board 和 Panel 清单。
- 新增受限符号/封装 `library_preview` 图像和官方 `library_classification_query` 分类树；扩展 `project_info` 返回原理图和 PCB 清单。
- 新增 `design_archive_export`，支持受限且明确授权的原生工程/文档归档读取，不写入文件系统。
- 在 `eda_context` 中加入当前画布数据单位，并通过 `pcb_constraints_query` 暴露当前/默认 PCB 规则配置名称。
- 新增受确认保护的 `pcb_constraints_manage`，支持受限修改网类、差分对、等长组和焊盘对组并读取验证。
- 从 0.4.15 公共接口中移除不受支持的 `schematic_net_query` 和 `pcb_layer_query.current` 路由。
- 调用需要参数的 `pcb_Document.save(uuid)` API 前先解析当前 PCB UUID。
- 运行时支持时，在 `eda_context` 中加入客户端版本、模式、编辑器版本和编译日期。
- 扩展 `pcb_document_action`，支持受限图元 ID/类型/BBox 查询、鼠标位置和明确选择控制。
- 新增结构化 PCB 和原理图 DRC 工具，默认不依赖界面状态。
- 固定版本 API 未公开相应方法前，暂缓 PCB 自动布局和自动布线 MCP 工具；仍可通过 `pcb_document_action` 使用外部布线/布局文件。
- 扩展 `library_search`，支持官方批量 LCSC C 编号映射；固定版本客户端不支持仿真模型搜索，因此不在 0.4.15 接口上宣传。
- 新增 PCB/原理图网络查询、PCB 约束、工程信息、网表比较和白名单制造导出。
- 新增精确器件属性搜索、PCB 图层查询、实时 DRC 控制、外部布线/布局导入和飞针制造导出。
- 新增只读 BOM 模板发现及 PCB/原理图 BOM 导出的模板选择。
- 新增精确器件、符号和封装库搜索 `library_search`。
- 扩展 `pcb_document_action`，支持受限图元/选择检查、坐标转换和画布导航。
- 新增器件、符号和封装库资源的精确 UUID 获取。
- 扩展 `pcb_constraints_query`，支持规则配置、网络规则、网络间规则和区域规则。
- 新增 `pcb_document_action`，支持 PCB 保存、原理图变更导入以及 Base64 自动布局/布线文件导入。
- 在模板发现和导出中加入原理图 BOM 装配变体配置。
- 为制造导出加入受限 JSON/SES 文本预览。

### 修复

- 将画布快照作为 MCP 图像内容返回，并通过 Server 和 Bridge 传递公开的快照/库/工作区超时控制。
- 长时间运行的 EDA 修改超时后继续隔离，直到不可取消的 Promise 完成。
- 将网络标签处理器的超时传递给运行时隔离机制。
- 使选择器和分析架构与处理器互斥规则及官方图元枚举一致；在受限序列化前保留完整的 LCSC 匹配数量。
- 规范化部分 0.4.15 EDA 构建返回的对象形式差分对读回数据。

### 变更

- 将 `@jlceda/pro-api-types` 从 `0.2.15` 升级到 `0.4.15` 以匹配当前 EDA Pro API 声明。这是编译期类型更新，不改变 EDA 客户端运行时实现。

---

## [2.0.9] - 2026-08-23

### 修复

- EDA 修改超时后继续隔离，直到不可取消的 API Promise 完成，避免后续读写与挂起的修改重叠。
- 将网络标签处理器的超时传递给运行时隔离机制。

### 变更

- 将 `@jlceda/pro-api-types` 从 `0.2.15` 升级到 `0.4.15` 以匹配当前 EDA Pro API 声明。这是编译期类型更新，不改变 EDA 客户端运行时实现。

---

## 未发布


## [2.0.8] - 2026-08-23

### 修复

- 自动放置器件时保留单独提供的 `x` 和 `y` 坐标。
- 通过受保护的强制客户端切换，从失效的活动 Bridge 任务中恢复。

### 变更

- 通过官方 EasyEDA 库 API 支持直接查询 LCSC C 编号。

---

### 修复

- `component_place_auto` 现在保留单独提供的 X 或 Y 坐标，并仅对缺失轴使用布局策略计算的回退坐标。
- 恢复 Bridge 的角色与租约检查；故障恢复切换后，旧活动页面中尚未开始的排队任务不会继续执行。

### 变更

- `component_select` 现在识别 LCSC C 编号，并通过官方 `eda.lib_Device.getByLcscIds` 直接解析已关联的 EasyEDA 器件；未关联的 C 编号会明确报告无法获取可放置的符号和封装。
---

## [2.0.7] - 2026-08-18

### 发布修复

- 在扩展 README 中加入功能演示图，展示原理图读取、器件放置、网络标签修改和 Bridge 连接状态。
- 将演示图包含在 `.eext` 发布包中，满足扩展广场的功能展示要求。

---

## [2.0.6] - 2026-08-17

### 修复

- 拒绝显式空 UUID 选择，避免自动布局或自动布线意外作用于整张图。
- EDA 处理器超时后保持串行，直到底层 Promise 真正结束。
- 使用每次调用的 API 超时值，并停止为常规心跳持久化调试日志。
- 任务真正开始执行时上报确认，使排队时间不占用 API 执行超时预算。
- `netlabel_modify` 可修改普通标签和组件形式的电源/地网络标识。

---

## [2.0.5] - 2026-08-17

### 发布准备

- 改为原生 MCP Server 安装说明，移除旧版 MCP Hub 引导和缺失的演示图片引用。
- 增加安全边界、兼容性和 `createNetLabel` Alpha API 已知限制说明。
- 社区与安全联系邮箱更新为 `hs150521@proton.me`。
- 配套发布可全局安装的 JLCEDA MCP Server 2.1.3 软件包。

---

## [2.0.4] - 2026-08-17

### 修复

- 将无响应的 JLCEDA `createNetLabel` alpha API 限制在 5 秒内失败，避免调用者等待全局任务超时。

### 变更

- 显示名称简化为「MCP Bridge 社区版」；非官方与上游归属声明继续保留在描述和 README 中。

---

## [2.0.3] - 2026-08-17

### 修复

- 网络标签通过公共 SDK getter 读取引脚和属性状态，并修正附近区域查询参数顺序。
- 普通标签批次失败时返回失败状态；自动器件创建返回 `undefined` 时不再误报成功。
- 为 Bridge 任务增加超时隔离，避免单个 BETA API 永久阻塞后续任务。

### 变更

- 显示名称改为「MCP Bridge 社区版（非官方）」，并明确标注基于上游 MCP Bridge 改进。

---

## [2.0.2] - 2026-08-17

### 修复

- 普通信号网络改用 `sch_PrimitiveAttribute.createNetLabel`，不再错误创建 Power 网络标识。
- 增加普通信号、电源轨和地网络分类测试。

---

## [1.5.4] - 2026-04-04

### 修复

- 修复器件放置期间点击鼠标右键取消放置时，只能等待超时才能继续的问题；现在轮询到浮动图元消失时立即返回取消状态，桥接端跳过当前器件继续下一个。

---

## [1.5.2] - 2026-04-02

### 变更

- 版本与服务端 1.5.2 保持同步。

---

## [1.5.1] - 2026-04-01

### 变更

- 版本与服务端 1.5.1 保持同步。

---

## [1.5.0] - 2026-04-01

### 新增

- 器件选型面板弹出时显示超时倒计时，最后 1 分钟自动标红，用户可随时了解剩余操作时间。
- 审查原理图时会自动检查连线规则，AI 在报告中会注明原理图是否存在连线错误。

### 优化

- 器件选型面板选中行高亮优化，深色主题下颜色对比度更佳，不再出现文字难以辨认的情况。

### 变更

- 版本与服务端 1.5.0 保持同步。
- 服务端（VS Code 扩展）显示名统一为「JLCEDA MCP Hub」，Marketplace 链接已同步更新。

---

## [1.4.1] - 2026-03-31

### 新增

- 全工程原理图审查功能，可一次性读取工程内所有原理图页面的器件与网络连接，适合多页电路的全局审查、完整 BOM 核查和跨页信号追踪。

---

## [1.4.0] - 2026-03-31

### 变更

- 扩展名称由「MCP 连接器」更名为「MCP Bridge」，菜单入口、连接设置页与状态说明同步更新。
- 原理图读取功能重新整合，现在可直接获取器件列表与引脚网络连接关系，数据来自 EDA 实时状态，刚放置或修改的器件立即可见。

---

## [1.3.4] - 2026-03-27

### 变更

- 无功能变化，与主服务端版本保持同步.

---

## [1.3.3] - 2026-03-25

### 变更

- 原理图检查能力拆分为两类，分别用于自动连线分析和功能性电路审查。

---

## [1.3.2] - 2026-03-25

### 变更

- 无功能变化，与主服务端版本保持同步.

---

## [1.3.1] - 2026-03-25

### 变更

- 无用户可见变更。

---

## [1.3.0] - 2026-03-24

### 新增

- 器件选型：AI 搜索到候选器件后，会在侧边栏以列表形式展示，等待用户确认后再放置。
- 器件放置：AI 可按顺序引导用户在原理图中逐个放置器件，侧边栏实时显示当前进度。

### 变更

- 取消器件选型或放置时，仅跳过当前器件，后续器件继续处理，不再整体中断。
- 电源符号和地符号不再由 AI 自动放置，改为提示用户手动添加。
- 器件选型或放置完成后，侧边栏会自动收起（需在设置中开启"打开 EDA 时关闭侧边栏"）。

---

## [1.2.0] - 2026-03-21

### 新增

- 现在可以直接让 AI 对原理图做一次完整检查，并返回更精简、更容易阅读的结果。

### 优化

- 启动等待时间延长到 30 秒，软件刚启动或电脑较慢时也更容易连上。
- 服务尚未启动时，状态提示会明确显示正在等待启动，不再用容易误解的超时提示。
- 连接状态提示按使用场景重新整理，页面展示更清楚。

### 修复

- 首次打开聊天时，如果连接还在建立，会先等待连接完成再执行操作，减少首次报错。
- 修复部分状态提示偶发显示不准确的问题。

---

## [1.1.3] - 2026-03-20

### 变更

- 原理图功能性审查速度更快，AI 能更高效地获取电路全貌。
- ERC/DRC 检查结果判断更准确，不再出现检查已通过但仍反复尝试获取详细结果的情况。
- AI 进行功能性审查时不会生成多余的文件下载链接。

---

## [1.1.2] - 2026-03-19

### 变更

- 服务端用户自定义指令由覆盖模式改为追加模式：内置指令始终有效，用户自定义指令在其基础上叠加生效，不再互相冲突。

---

## [1.1.1] - 2026-03-19

### 新增

- EDA Bridge 与 MCP 服务端版本不一致时，在 VS Code 右下角弹出提示，告知哪一侧版本较低并建议升级。

---

## [1.1.0] - 2026-03-18

### 新增

- 侧边栏底部新增打开 EDA 时自动收起侧边栏的开关，避免启动 EDA 后侧边栏遮挡视野。

### 变更

- 优化：AI 在原理图中放置器件时，会自动将器件布局在图纸中心区域，不再出现摆到边角或超出红框的情况。
- 优化：AI 放置完器件后会自动完成所有引脚的电气连接，不再留悬空引脚要用户手动补线。
- 新增：用户说检查原理图时，AI 默认进行功能性审查（分析电路用途、器件选型、引脚连接、能否正常工作），只有明确说跑 ERC 或做 DRC 时才执行规则检查。

---

## [1.0.0] - 2026-03-16

### 当前功能

**安装与激活**

- 扩展安装后随嘉立创 EDA 启动自动激活，无需手动触发。
- 在首页、原理图、PCB 顶部菜单栏注入「MCP Bridge」菜单，提供「连接设置」和「关于」入口。

**连接设置**

- 支持配置桥接服务地址，地址格式实时校验，非法格式拒绝保存并提示错误。
- 设置页实时显示桥接连接状态和 WebSocket 连接状态。
- 配置变更后自动重新连接，无需重启 EDA。

**桥接连接**

- 激活后自动连接到 MCP 服务端，断线后自动重连。
- 支持多个 EDA 实例同时接入，由服务端自动裁决哪个实例执行任务。
- 当前页面不是原理图或 PCB 编辑页时，连接自动暂停。

**EDA 工具支持**

- 支持 API 检索、API 调用、当前上下文获取三个 AI 工具，AI 通过这三个工具操控嘉立创 EDA。
